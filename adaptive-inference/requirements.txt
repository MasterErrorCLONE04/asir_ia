# ─── R1.0 runtime ────────────────────────────────────────────────────────────
# tasks/generator.py and tasks/tokenizer.py use only stdlib.
# No third-party packages are required to run generate_data.py or the tests.

# ─── Development-phase baseline (R1.x) ───────────────────────────────────────
# These are pinned here so the image is reproducible and future training phases
# don't need a rebuild.  CPU-only PyTorch keeps the image lean; CUDA support
# will be introduced in a separate stage when we reach R1.5+.

# Core numeric stack
numpy>=1.26,<2.0
scipy>=1.12,<2.0
pandas>=2.2,<3.0

# Deep-learning framework (CPU wheel – no CUDA deps in this image)
torch>=2.3,<3.0
torchvision>=0.18,<1.0   # kept in sync with torch; useful for later vision tasks

# Serialisation / config
PyYAML>=6.0,<7.0

# Testing
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0     # coverage reports from the start

# ─── Future phases (uncomment as needed) ─────────────────────────────────────
# transformers>=4.40,<5.0
# datasets>=2.19,<3.0
# accelerate>=0.30,<1.0
# wandb>=0.17,<1.0
